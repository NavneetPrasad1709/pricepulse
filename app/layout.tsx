import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "PricePulse- Real-Time Price Tracking for E-commerce",
  description: "Created by Navneet Prasad, PricePulse is a real-time price tracking tool for e-commerce platforms. It helps users monitor product prices, receive instant alerts on price drops, and make informed purchasing decisions. With a user-friendly interface and powerful features, PricePulse is your go-to solution for smart shopping.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
